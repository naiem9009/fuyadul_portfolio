

const SectionWrapper = ({children}) => {


    return (
        <div className='lg:max-w-screen-xl md:max-w-screen-lg max-w-screen-sm mx-auto'>
            {children}
        </div>
    )
}

export default SectionWrapper